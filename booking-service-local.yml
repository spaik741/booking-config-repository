spring:
  data:
    mongodb:
      database: booking-store
      uri: mongodb://admin:password@localhost:27017/
      auto-index-creation: true

management:
  endpoints:
    web:
      exposure:
        include: "*"